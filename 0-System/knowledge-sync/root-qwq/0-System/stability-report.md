# 系统稳定性报告

**事件**: 服务意外退出  
**时间**: 2026-03-07 11:59  
**状态**: ✅ 已恢复并加固

---

## 📊 事件分析

### 现象
```
[server exited unexpectedly]
```

### 原因
U22 容器资源限制导致服务意外退出：
- 内存压力：Swap 使用 6.5G/15G
- 磁盘紧张：93% 使用率
- 容器保活机制限制

### 影响评估

| 方面 | 影响 | 状态 |
|------|------|------|
| **数据完整性** | 无影响 | ✅ 所有数据已保存 |
| **对话历史** | 无影响 | ✅ 已存储到数据库 |
| **知识库** | 无影响 | ✅ 文件完整 |
| **监控数据** | 无影响 | ✅ 已持久化 |
| **PWA 缓存** | 无影响 | ✅ Service Worker 正常 |

---

## ✅ 已实施防护

### 1. 服务守护脚本

**位置**: `/root/air/qwq/scripts/ai-team-hub.sh`

**功能**:
- ✅ 每 30 秒自动检查服务
- ✅ 服务异常自动重启
- ✅ 详细日志记录
- ✅ PID 文件管理

**启动方式**:
```bash
# 后台启动守护进程
nohup /root/air/qwq/scripts/ai-team-hub.sh > /tmp/ai-team-hub-watchdog.log 2>&1 &
```

### 2. 日志管理

| 日志 | 路径 | 用途 |
|------|------|------|
| 服务日志 | `/tmp/knowledge-service.log` | uvicorn 运行日志 |
| 守护日志 | `/tmp/ai-team-hub-watchdog.log` | 守护进程日志 |

### 3. 快速恢复命令

```bash
# 检查服务状态
curl -s http://localhost:8772/api/health

# 手动重启服务
cd /root/air/qwq/mobile-web
nohup uvicorn app:app --host 0.0.0.0 --port 8772 > /tmp/knowledge-service.log 2>&1 &

# 查看日志
tail -f /tmp/knowledge-service.log
```

---

## 📈 系统资源状态

### 当前状态
| 资源 | 使用 | 可用 | 状态 |
|------|------|------|------|
| 磁盘 | 442G | 37G | ⚠️ 93% |
| 内存 | 8.5G | 6.5G | ⚠️ Swap 使用 |
| Swap | 6.5G | 9.5G | ⚠️ 压力高 |

### 优化建议

#### 立即执行
1. **清理 PM2 日志**: `pm2 flush`
2. **清理临时文件**: `rm -rf /tmp/*.log`
3. **清理旧服务**: 停止不用的 PM2 进程

#### 本周执行
1. **磁盘清理**: 删除旧日志和缓存
2. **内存优化**: 减少并发进程
3. **监控告警**: 设置资源阈值通知

---

## 🛡️ 稳定性保障

### 多层防护

```
┌─────────────────────────────────────┐
│  用户层                              │
│  - 服务退出提示                      │
│  - 自动重试机制                      │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  应用层                              │
│  - 守护进程监控                      │
│  - 30 秒自动检查                     │
│  - 异常自动重启                      │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  系统层                              │
│  - PM2 进程管理                      │
│  - 系统资源监控                      │
└─────────────────────────────────────┘
```

### 恢复时间目标 (RTO)

| 场景 | 目标 | 实际 |
|------|------|------|
| 服务意外退出 | <1 分钟 | ✅ 30 秒内 |
| 容器重启 | <5 分钟 | ✅ 自动启动 |
| 系统故障 | <10 分钟 | ✅ 手动恢复 |

---

## 📋 运维清单

### 每日检查
- [ ] 服务健康检查 `curl http://localhost:8772/api/health`
- [ ] 查看守护日志 `tail /tmp/ai-team-hub-watchdog.log`
- [ ] 检查磁盘空间 `df -h /`

### 每周维护
- [ ] 清理日志文件
- [ ] 检查内存使用
- [ ] 审查服务重启次数

### 每月优化
- [ ] 性能分析
- [ ] 资源优化
- [ ] 架构审查

---

## 🚨 故障处理流程

### 1. 服务不可用

```bash
# 检查服务
curl -s http://localhost:8772/api/health

# 如无响应，重启服务
cd /root/air/qwq/mobile-web
nohup uvicorn app:app --host 0.0.0.0 --port 8772 > /tmp/knowledge-service.log 2>&1 &

# 验证恢复
curl -s http://localhost:8772/api/health
```

### 2. 守护进程异常

```bash
# 检查守护进程
pgrep -af "ai-team-hub"

# 重启守护进程
nohup /root/air/qwq/scripts/ai-team-hub.sh > /tmp/ai-team-hub-watchdog.log 2>&1 &
```

### 3. 资源耗尽

```bash
# 清理磁盘
pm2 flush
rm -rf /tmp/*.log

# 释放内存
pkill -f "python.*idle"
```

---

## 📞 支持信息

### 日志位置
- 服务日志：`/tmp/knowledge-service.log`
- 守护日志：`/tmp/ai-team-hub-watchdog.log`
- 系统日志：`/var/log/syslog`

### 联系方式
- 问题报告：`shared-tasks/inbox/`
- 紧急联系：查看系统联系人

---

*报告生成：2026-03-07 12:15*  
*下次审查：2026-03-14*  
*状态：✅ 服务正常运行*
